# edge/docker/docker-compose.ymlのgrafanaサービス、およびmake up実行時のURL/認証情報表示が参照する
# (リポジトリルートに置いているのは、Makefileの各種targetから常に同じ相対パスで参照できるようにするため)。
# 実際に使う値は .env にコピーして設定すること(.envはgitignore対象、このファイルはテンプレートとしてgit管理下)。
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=admin
GRAFANA_PORT=3001
