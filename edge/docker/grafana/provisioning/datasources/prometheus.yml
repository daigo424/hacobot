# GrafanaにPrometheusデータソースを自動登録する(手動でのUI設定を不要にする)。
# network_mode: hostで動いているため、Prometheus自体もlocalhost:9090で見える。
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://localhost:9090
    isDefault: true
    editable: true
